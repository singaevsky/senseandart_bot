## 📄 Как создать файл credentials.json для Google Sheets

Файл `credentials.json` содержит учетные данные сервисного аккаунта Google. Вот пошаговая инструкция:

---

## 🔧 Вариант 1: Создание Service Account (Рекомендуется)

### Шаг 1: Перейдите в Google Cloud Console
1. Откройте [Google Cloud Console](https://console.cloud.google.com/)
2. Выберите существующий проект или создайте новый

### Шаг 2: Включите Google Sheets API
1. В меню слева выберите **"APIs & Services"** → **"Library"**
2. Найдите **"Google Sheets API"**
3. Нажмите **"Enable"**

### Шаг 3: Создайте Service Account
1. Перейдите в **"APIs & Services"** → **"Credentials"**
2. Нажмите **"Create Credentials"** → **"Service account"**
3. Заполните поля:
   - **Service account name**: `sheets-access` (или любое имя)
   - **Service account ID**: автоматически заполнится
   - **Service account description**: Описание (опционально)
4. Нажмите **"Create and Continue"**

### Шаг 4: Назначьте роль
1. Выберите роль: **"Basic"** → **"Editor"**
2. Нажмите **"Continue"**

### Шаг 5: Создайте ключ
1. На следующем экране нажмите **"Done"**
2. Найдите созданную учетную запись в списке
3. Нажмите на неё
4. Перейдите на вкладку **"Keys"**
5. Нажмите **"Add Key"** → **"Create new key"**
6. Выберите **"JSON"** формат
7. Нажмите **"Create"**

### Шаг 6: Скачайте файл
1. Файл автоматически скачается как `project-name-xxxxx.json`
2. Переименуйте его в `credentials.json`
3. Поместите в корень проекта

---

## 🔐 Вариант 2: Если у вас уже есть проект

Если у вас есть доступ к существующему проекту:

### Шаг 1: Скачайте существующий ключ
1. Перейдите в **"APIs & Services"** → **"Credentials"**
2. Найдите Service Account Key
3. Нажмите на три точки → **"Manage keys"**
4. Нажмите **"Add Key"** → **"Create new key"**
5. Выберите **"JSON"** и скачайте

### Шаг 2: Или используйте существующий ключ
Если ключ уже существует, просто скачайте его и переименуйте в `credentials.json`

---

## 🛡️ Безопасность

### Важные моменты:
1. **Никогда не публикуйте** `credentials.json` в репозитории
2. Добавьте файл в `.gitignore`:
   ```gitignore
   credentials.json
   *.json.key
   ```
3. Ограничьте права доступа к файлу:
   ```bash
   chmod 600 credentials.json
   ```

### Ротация ключей:
1. Регулярно создавайте новые ключи
2. Удаляйте старые неиспользуемые ключи
3. Мониторьте использование ключей в консоли

---

## 🔗 Настройка доступа к таблице

После создания `credentials.json`:

### Шаг 1: Откройте Google Таблицу
https://docs.google.com/spreadsheets/d/1V4R...............................E0/edit

### Шаг 2: Предоставьте доступ
1. Нажмите **"Share"** (Поделиться)
2. Добавьте email сервисного аккаунта:
   ```
   sal.....rt@co...............ery-e......u.iam.gserviceaccount.com
   ```
3. Выберите роль **"Editor"** (Редактор)
4. Нажмите **"Send"** или **"Готово"**

---

## 📋 Пример содержимого credentials.json

```json
{
  "type": "service_account",
  "project_id": "your-project-id",
  "private_key_id": "your-private-key-id",
  "private_key": "-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n",
  "client_email": "your-service-account@your-project.iam.gserviceaccount.com",
  "client_id": "your-client-id",
  "auth_uri": "https://accounts.google.com/o/oauth2/auth",
  "token_uri": "https://oauth2.googleapis.com/token",
  "auth_provider_x509_cert_url": "https://www.googleapis.com/oauth2/v1/certs",
  "client_x509_cert_url": "https://www.googleapis.com/robot/v1/metadata/x509/your-service-account%40your-project.iam.gserviceaccount.com"
}
```

---

## 🧪 Проверка работы

### Тестовый скрипт:
```python
# test_credentials.py
import gspread
from google.oauth2 import service_account

# Проверка файла
try:
    credentials = service_account.Credentials.from_service_account_file(
        'credentials.json',
        scopes=['https://www.googleapis.com/auth/spreadsheets']
    )
    
    gc = gspread.authorize(credentials)
    print("✅ Аутентификация успешна!")
    
    # Проверка доступа к таблице
    spreadsheet = gc.open_by_key("1V4RGu...............................UInE0")
    print("✅ Доступ к таблице получен!")
    
    worksheet = spreadsheet.sheet1
    print("✅ Лист доступен!")
    
except Exception as e:
    print(f"❌ Ошибка: {e}")
```

---

## ⚠️ Возможные ошибки

### "Permission denied"
- Проверьте, что email сервисного аккаунта добавлен в таблицу
- Убедитесь, что выбрана роль "Editor"

### "Invalid grant"
- Ключ может быть поврежден
- Создайте новый ключ

### "File not found"
- Проверьте путь к файлу `credentials.json`
- Убедитесь, что файл существует

---

## 🎯 Готово!

Теперь у вас есть:
- ✅ Файл `credentials.json` для доступа к Google Sheets
- ✅ Настроенный сервисный аккаунт
- ✅ Права доступа к таблице
- ✅ Безопасная конфигурация

Файл готов к использованию в проекте! 🚀
