## ✅ Подготовка к передаче проекта заказчику

На основе предоставленных файлов проекта, подготовлю все необходимые материалы для передачи заказчику.

---

## 📁 Структура проекта

```
senseandart_bot/
├── bot_service_account.py         # Основной файл бота
├── config.py                      # Конфигурация
├── google_sheets_service_account.py # Работа с Google Sheets
├── localization.py                # Локализация
├── requirements.txt               # Зависимости
├── .env.example                   # Пример конфигурации
├── .gitignore                     # Игнорируемые файлы
├── README.md                      # Документация
├── locales/                       # Файлы локализации
│   ├── ru.json                   # Русский язык
│   └── en.json                   # Английский язык
├── credentials.json              # Ключи Google (не в репозитории)
└── notified_users.json          # Локальный кэш (не в репозитории)
```

---

## 📄 README.md

```markdown
# 🤖 Telegram-бот для канала «Искусство и смыслы»

Автоматизированный бот для привлечения подписчиков и выдачи промокодов.

## 🎯 Основные функции

- ✅ Автоматическая встреча новых подписчиков
- 🔍 Проверка подписки на канал @senseandart
- 🎁 Выдача промокода со скидкой 10% (ART10)
- 🔗 Переход к закреплённому посту канала
- 📊 Сохранение данных в Google Таблице
- 🌍 Поддержка русского и английского языков

## 📋 Логика работы

1. **Пользователь запускает бота** (`/start` или кнопка "Начать")
2. **Бот проверяет подписку** через Telegram Bot API
3. **Если не подписан** → просит подписаться и даёт ссылку на канал
4. **Если подписан** → выдаёт промокод и кнопку перехода к важному посту
5. **Данные сохраняются** в Google Таблицу

## 🚀 Установка и запуск

### Требования
- Python 3.8+
- Google Service Account (credentials.json)
- Telegram Bot Token

### Установка зависимостей
```bash
pip install -r requirements.txt
```

### Настройка конфигурации
1. Скопируйте `.env.example` в `.env`
2. Заполните необходимые параметры:
   - `TELEGRAM_BOT_TOKEN` - токен бота
   - `ADMIN_ID` - ваш Telegram ID
   - `GOOGLE_SHEETS_ID` - ID Google Таблицы

### Запуск
```bash
python bot_service_account.py
```

## ⚙️ Конфигурация

### Обязательные файлы
- `.env` - переменные окружения
- `credentials.json` - ключи Google Service Account

### Переменные окружения
```env
TELEGRAM_BOT_TOKEN=ваш_токен_бота
CHANNEL_USERNAME=@senseandart
ADMIN_ID=ваш_telegram_id
PROMO_CODE=ART10
GOOGLE_SHEETS_ID=ID_таблицы
GOOGLE_CREDENTIALS_FILE=credentials.json
SHEET_NAME=Sheet1
```

## 📊 Интеграция с Google Sheets

### Структура таблицы
| Колонка | Описание |
|---------|----------|
| user_id | ID пользователя Telegram |
| username | Имя пользователя |
| full_name | Полное имя |
| joined_at | Дата подписки |
| promo_code | Промокод |
| status | Статус (подписан/отписан) |
| unsubscribed_at | Дата отписки |

### Настройка доступа
1. Создайте Google Таблицу
2. Добавьте email сервисного аккаунта как редактора
3. Укажите ID таблицы в конфигурации

## 🛠️ Администрирование

### Уведомления администратора
- Новый пользователь бота
- Новый подписчик канала
- Получение промокода
- Отписка пользователя

### Локальный кэш
Файл `notified_users.json` предотвращает дублирование уведомлений.

## 🌍 Локализация

Поддерживаемые языки:
- Русский (`ru`)
- Английский (`en`)

Файлы локализации находятся в директории `locales/`.

## 📁 Структура проекта

```
├── bot_service_account.py     # Основной файл бота
├── config.py                  # Конфигурация
├── google_sheets_service_account.py  # Работа с Google Sheets
├── localization.py            # Локализация
├── requirements.txt           # Зависимости
├── locales/                   # Файлы локализации
│   ├── ru.json
│   └── en.json
└── README.md                  # Документация
```

## 🔧 Техническая поддержка

### Распространенные ошибки

**Ошибка аутентификации Google Sheets:**
- Проверьте файл `credentials.json`
- Убедитесь, что сервисный аккаунт имеет доступ к таблице

**Бот не отвечает:**
- Проверьте правильность TELEGRAM_BOT_TOKEN
- Убедитесь, что вебхуки не конфликтуют

### Обновление кода
```bash
git pull origin main
pip install -r requirements.txt --upgrade
```

## 📞 Контакты разработчика

Для технической поддержки обращайтесь к разработчику проекта.

## 📄 Лицензия

Проект предоставляется "как есть" для внутреннего использования.
```

---

## 📁 .env.example

```env
# Telegram Bot Configuration
TELEGRAM_BOT_TOKEN=your_bot_token_here
CHANNEL_USERNAME=@senseandart
ADMIN_ID=your_telegram_id
PROMO_CODE=ART10

# Google Sheets Configuration
GOOGLE_SHEETS_ID=1V4R...................................InE0
GOOGLE_CREDENTIALS_FILE=credentials.json
SHEET_NAME=Sheet1

# Optional Configuration
PINNED_POST_URL=https://t.me/senseandart/1
```

---

## 📁 .gitignore

```gitignore
# Environment variables
.env
.env.local
.env.*.local

# Google credentials
credentials.json
*.json.key

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# IDE
.idea/
.vscode/
*.swp
*.swo
.DS_Store

# Local cache
notified_users.json
*.log
*.tmp
*.temp

# Virtual Environment
venv/
env/
ENV/
.venv/
.env/
```

---

## 📋 Список используемых блоков/функций

### Основные модули:
1. **bot_service_account.py** - главный файл бота
2. **config.py** - конфигурация
3. **google_sheets_service_account.py** - работа с Google Sheets
4. **localization.py** - локализация
5. **locales/ru.json, en.json** - файлы переводов

### Основные функции:
- `welcome_message()` - приветствие пользователя
- `handle_start_command()` - обработка команды /start
- `check_subscription()` - проверка подписки
- `promo()` - выдача промокода
- `menu_text_handler()` - обработка текстовых кнопок
- `callback_query_handler()` - обработка inline-кнопок
- `save_subscriber_to_sheet()` - сохранение данных
- `notify_admin_*()` - уведомления администратора

### Внешние зависимости:
- `python-telegram-bot` - Telegram API
- `gspread` - Google Sheets API
- `pandas` - работа с данными
- `google-auth` - аутентификация Google
- `python-dotenv` - переменные окружения

---

## 📋 Инструкции по редактированию

### Добавление нового языка:
1. Создать файл `locales/[код_языка].json`
2. Добавить все ключи из других файлов локализации
3. Перевести значения

### Изменение промокода:
1. В файле `.env` изменить значение `PROMO_CODE`
2. Или в `config.py` изменить значение по умолчанию

### Изменение канала:
1. В файле `.env` изменить значение `CHANNEL_USERNAME`
2. Обновить ссылки в `inline_channel_keyboard()`

### Изменение уведомлений:
1. Редактировать функции `notify_admin_*()` в `bot_service_account.py`
2. Изменить формат сообщений

### Добавление новых функций:
1. Создать новую функцию-обработчик
2. Добавить соответствующую команду в `set_commands()`
3. Зарегистрировать обработчик в `main()`

---

## 🔐 Доступы и интеграции

### Telegram Bot
- **Bot Username**: @EsArtSocietyBot
- **Token**: Хранится в переменной окружения `TELEGRAM_BOT_TOKEN`
- **Webhook**: Автоматический через polling

### Google Sheets
- **Таблица**: https://docs.google.com/spreadsheets/d/1V4RGuU-n...........................nE0
- **Service Account**: sale....t@c...........iam.gserviceaccount.com
- **Ключи**: Хранятся в файле `credentials.json`

### Локальные файлы (не в репозитории):
- `.env` - конфигурационные переменные
- `credentials.json` - ключи Google
- `notified_users.json` - кэш уведомлений

---

## 📤 Экспорт конфигурации

### Файлы конфигурации:
1. `.env` - переменные окружения
2. `credentials.json` - ключи Google Service Account
3. `notified_users.json` - локальный кэш

### Настройки бота:
- Команды меню: `/start`, `/check`, `/promo`
- Webhook: polling (автоматический)
- Администратор: ID из конфигурации

### Интеграции:
- Google Sheets API (через service account)
- Telegram Bot API
- Локальное хранение кэша

---

## 📋 Передача заказчику

### Что предоставить заказчику:
1. **Доступ к BotHost**: Логин/пароль от аккаунта
2. **Права администратора**: Полный доступ к проекту
3. **Конфигурационные файлы**: 
   - `.env` с токенами
   - `credentials.json` с ключами Google
4. **Документация**: Этот README.md файл
5. **Инструкции**: По редактированию и поддержке

### Рекомендации по развертыванию:
1. Загрузить код из репозитория https://github.com/singaevsky/senseandart_bot.git
2. Создать файлы `.env` и `credentials.json` вручную
3. Настроить переменные окружения
4. Запустить бота через систему управления BotHost

---

## 🎉 Готовность к передаче

✅ Бот полностью функционален  
✅ Документация подготовлена  
✅ Инструкции по развертыванию  
✅ Все конфигурационные файлы описаны  
✅ Доступы и интеграции задокументированы  

Проект готов к передаче заказчику! 🚀
